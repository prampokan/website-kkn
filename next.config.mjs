/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['firebasestorage.googleapis.com', 'lh3.googleusercontent.com', 'encrypted-tbn0.gstatic.com', 'pramudya-diagusta.vercel.app']
    }
};

export default nextConfig;
